## Feature 003: Add Node Discovery Response

When a node asks for the nodes on a network, return the list discoverable nodes on the network.

**Tasks**

**Dependencies**
Ghost Message Protocol [[gmp/feature-003](ghost-network-common/feature-003.md)  :green_circle:

**Commits**

feat(feature-003): add node discovery (feat(feature-003): add node discovery)